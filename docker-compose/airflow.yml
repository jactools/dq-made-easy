# ============================================================================
# AIRFLOW: Apache Airflow (platform-managed lifecycle)
#
# Airflow container lifecycle managed by platform-foundation.
# dq-made-easy retains only DQ-specific configuration for DAG deployment.
# Consumers deploy DAGs via Airflow API (no mounts).
# ============================================================================

# No services defined here — Airflow runs from platform-foundation/docker-compose/airflow.yml.
# DQ-specific Airflow configuration retained for DAG deployment scripts:
#   DQ_AIRFLOW_BASE_URL, DQ_AIRFLOW_USERNAME, DQ_AIRFLOW_PASSWORD, etc.
