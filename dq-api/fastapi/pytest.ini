[pytest]
pythonpath = .
testpaths = tests
addopts = --cov --cov-config=../.coveragerc --cov-report=term-missing --cov-report=xml --cov-report=json
markers =
	integration: marks tests that require the live Postgres container (skip with -m "not integration")
