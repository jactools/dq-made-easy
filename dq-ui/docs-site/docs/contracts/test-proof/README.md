# Test Proof

This contract package defines the canonical JSON schema for curated UI and API test proof summaries stored under `test-results/test-proof/`.

Use this schema for committed proof artifacts that are meant to be reviewed, published, and linked from the generated test-proof docs.

The source of truth is `v1/schema.json`.