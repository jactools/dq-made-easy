apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - api.yaml
  - db.yaml
  - kong.yaml
  - keycloak.yaml
  - kafka.yaml
  - airflow.yaml

transformers:
  - metadata/labels.yaml
  - metadata/annotations.yaml
