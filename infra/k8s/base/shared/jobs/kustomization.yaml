apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - validation.yaml
  - validation-scripts.yaml
  - dq-role-assignment.yaml

configMapGenerator:
- name: dq-role-assignment-script
  files:
    - dq-role-assignment.sh
