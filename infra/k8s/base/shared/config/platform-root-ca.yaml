apiVersion: v1
kind: ConfigMap
metadata:
  name: platform-root-ca
  labels:
    app.kubernetes.io/part-of: dq-made-easy
data:
  # Populated by generate_dev_certs.sh or Kustomize overlay
  rootCA.pem: |
    -----PLACEHOLDER-----
