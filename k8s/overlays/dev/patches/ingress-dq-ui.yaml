apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dq-ui
spec:
  tls:
    - hosts:
        - dq-ui.dev.jac.dot
      secretName: dq-ui.dev.jac.dot-tls
  rules:
    - host: dq-ui.dev.jac.dot