apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dq-ui
spec:
  tls:
    - hosts:
        - dq-ui.dev.jac.dot
      secretName: dq-ui-tls
  rules:
    - host: dq-ui.dev.jac.dot
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dq-frontend
                port:
                  number: 443